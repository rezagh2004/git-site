const login=(user)=>{
    if(user.name==="reza"){
        return true
    }else{
        return false
    }
}

const register=(user)=>{
    return {user , token}
}

const getme=(token)=>{
    return user
}

const test=()=> false

const addCourse=(course)=>{
    return {
        message:"new course added successfully :))"
    }
}

//Auth